const HOME = "/";
const UPLOAD = "/upload";
const FIND_ALL = "/find/all";
const FIND = "/find";

const routes = {
    home: HOME,
    upload: UPLOAD,
    findAll: FIND_ALL,
    find: FIND
};

export default routes;