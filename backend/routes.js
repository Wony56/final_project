const HOME = "/";
const UPLOAD = "/upload";
const UPLOAD_MANY = "/upload/many";
const FIND_ALL = "/find/all";
const FIND = "/find";

const routes = {
    home: HOME,
    upload: UPLOAD,
    uploadMany: UPLOAD_MANY,
    findAll: FIND_ALL,
    find: FIND,
};

export default routes;