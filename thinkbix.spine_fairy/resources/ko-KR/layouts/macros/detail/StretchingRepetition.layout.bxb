layout-macro-def (StretchingRepetition){
  params{
    param (stretchingSel){
      type (StretchingSel)
      min (Required)
      max (One)
    }
  }

  content{
    if(value(stretchingSel.repetition) != ' '){
    paragraph{
      value ("⏱#{value(stretchingSel.repetition)}")
      style(Detail_L)
    }
    }
  }
  
}
