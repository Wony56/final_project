layout-macro-def (StretchingCheck){
  params{
    param (stretchingSel){
      type (StretchingSel)
      min (Required)
      max (One)
    }
  }

  content{
    paragraph{
      value ("#{value(stretchingSel.check)}")
      // style(Label_M_Soft)
    }
  }
  
}

