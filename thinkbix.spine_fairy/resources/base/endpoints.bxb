endpoints {
  action-endpoints {
    // action-endpoint (GetStretching) {
    //   accepted-inputs (place, part)
    //   local-endpoint (GetStretching.js)
    // }
    action-endpoint (SelectStretchingSearch) {
      accepted-inputs (place, part)
      local-endpoint (SelectStretchingSearch.js)
    }
    action-endpoint (GetStretchingStep) {
      accepted-inputs (stretchingSel)
      local-endpoint (GetStretchingStep.js)
    }
  }
}
