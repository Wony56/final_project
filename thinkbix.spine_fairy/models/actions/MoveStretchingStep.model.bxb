action (NextStretchingStep) {
  description (다음 단계 설명 가져오기)
  type(Commit)
  collect {
    input (stretchingSel) {
      type (StretchingSel)
      min (Optional) max (One)
    }
    input (stretchingStep) {
      type (StretchingStep)
      min (Optional) max (One)
    }
  }
  output (StretchingStep)
}
